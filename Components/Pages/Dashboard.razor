@page "/dashboard"
@using CulinaryCommand.Components.Custom

<PageTitle>Dashboard</PageTitle>

<div class="container mt-4">
    <div class="row g-5">
        <div class="col-12 col-md-6">
            <DashboardCard Title="Prep Items Needing Attention" Value="5 items" Color="#ffc107" IconClass="bi bi-exclamation-triangle" Link="/prep" />
            <DashboardCard Title="Orders Pending Approval" Value="3 orders" Color="#688f72" IconClass="bi bi-file-earmark-check" Link="/orders" />
            <DashboardCard Title="Daily Summaries / Reports" Value="1" Color="#883ae1" IconClass="bi bi-file-text" Link="/reports" />
        </div>
        <div class="col-12 col-md-6">
            <DashboardCard Title="Active Tasks by Employee" Value="12 orders" Color="#3662e3" IconClass="bi bi-people" Link="/tasks" />
            <DashboardCard Title="Low Stock Ingredients" Value="12 orders" Color="#ca3a31" IconClass="bi bi-box-seam" Link="/inventory" />
            <DashboardCard Title="Manage Employees" Value="12 orders" Color="#e28743" IconClass="bi bi-person-gear" Link="/employees" />
        </div>
    </div>

    <div class="row">
        row 2
    </div>
</div>

@code {

}